# This repository uses env files per context:
# - Frontend vars: `packages/frontend/.env.example`
# - Supabase Edge Functions vars: `supabase/functions/.env.example`
#
# Keep this root file only as guidance to avoid duplicating keys.