{
  "name": "money-tracker",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "db:types": "supabase gen types typescript --local > packages/frontend/src/types/database.types.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}